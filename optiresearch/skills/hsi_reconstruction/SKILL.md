# HSI Reconstruction

Use this skill when a task requires synthetic hyperspectral data, PSF-based forward measurements, or reconstruction metrics such as PSNR, SSIM, SAM, and ERGAS.

The MVP command is `run_synthetic_hsi_reconstruction`. It uses a deterministic synthetic dataset and a numpy linear baseline.
