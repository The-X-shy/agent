# Evidence Review

Use this skill when a task asks whether a claim is supported, contradicted, simulation-only, or unsupported.

The reviewer must not upgrade mock-backed results to prototype validation. Claims produced from `mock_deeplens` scope require the caveat `currently simulation-only or mock-backed`.
